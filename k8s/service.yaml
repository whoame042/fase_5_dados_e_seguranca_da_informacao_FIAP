apiVersion: v1
kind: Service
metadata:
  name: vehicle-resale-api-service
  namespace: vehicle-resale
  labels:
    app: vehicle-resale-api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8082
    protocol: TCP
    name: http
  selector:
    app: vehicle-resale-api

