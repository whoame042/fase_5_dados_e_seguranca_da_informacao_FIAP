apiVersion: v1
kind: Secret
metadata:
  name: vehicle-resale-secret
  namespace: vehicle-resale
  labels:
    app: vehicle-resale-api
type: Opaque
data:
  # Senha do banco de dados (base64 encoded: "postgres123")
  DB_PASSWORD: cG9zdGdyZXMxMjM=
stringData:
  # Secret do cliente OIDC
  QUARKUS_OIDC_CREDENTIALS_SECRET: vehicle-resale-secret
