apiVersion: v1
kind: Service
metadata:
  name: keycloak-postgres-service
  namespace: vehicle-resale
  labels:
    app: keycloak-postgres
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: keycloak-postgres

